---
import '../layouts/style.css';
import Navigation from "../components/Navigation.astro";
import DevblogsContainer from "../components/DevblogsContainer.jsx"
import type { MarkdownInstance } from 'astro';
import { marked } from 'marked';
interface Frontmatter {
    title: string;
    description?: string;
}

const posts = Object.values(import.meta.glob<MarkdownInstance<Frontmatter>>('../content/*.md', { eager: true }));
---
<head>
    <meta charset="UTF-8">
    <title>Chris's Devlog</title>
</head>
<body>

<Navigation />
<DevblogsContainer/>
<!-- <div>
    {posts.reverse().map(post => 
    <div class="devlog_entry">
        <div>
            <h1 set:html={post.frontmatter.title}></h1>
        </div>
        <div>
            <h3 set:html={post.frontmatter.date}></h1>
        </div>
        <article set:html={marked.parse(post.rawContent())}></article>
    </div>
    )}
</div> -->

</div>
</body>
